# Agent Instructions

When editing files under `Hooks/`:
- Read `Hooks/VISION.md` first and keep changes aligned with that vision.
- Keep hook code as thin event-forwarding glue with safe lifecycle handling.
